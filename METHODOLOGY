always ask
    do I have creds?
    do I control something?
    can I reach a better machine?
    is a DA session available?

one-Line mental model
    Creds → ACL → Service Account → Server → DA Session → Token → DA
    User → ACL → group → service account → server → DA session → token

any time i land on a host:
    who am i?
    what privileges do i have?
    who is logged in?
    what credentials exist?
    what services run here?

    if DA is present:
        immediately think “token theft / credential extraction”

----------

always run:
    whoami /all
    ps
    getuid
    
    If LOCAL ADMIN:
        dump creds (Mimikatz / logonpasswords)
        check tokens (steal_token)
        check sessions (who’s logged in)
    NOTE: Local admin = credential access opportunity

enumeration priorities:
    credentials (fastest win)
    sessions (DA logged in?)
    ACL paths (bloodhound mindset)
    kerberos attack surface
    
ACL Abuse (IMPORTANT)
    GenericWrite
        Reset password
        Add SPN → Kerberoast
        Modify attributes
    WriteDACL
        Grant yourself GenericAll
        Then take over object
    GenericAll
        Full control → instant takeover

group abuse
    control group → add yourself
    have WriteDACL → modify ACL → then add yourself

service accounts (svc_*)
    target when:
        SPNs exist
        they run services
        they have privileges
    pattern:
        Service account → server → creds → DA

on a server
    dump creds (Mimikatz)
    check sessions
    look for DA
    steal token
        token theft
            ps
            steal_token <pid>
            getuid

ADCS (if present)
    if template has:
        client auth
        Low-priv enrollment
        “Supply in request”
    request cert as DA → authenticate → full compromise

gMSA
    extract password from host
    use for lateral movement
    often high privilege

-------DECISION TREE--------

1. foothold
    local admin?
        YES:
            dump creds (Mimikatz)
            check sessions
            check tokens
            👉 DA session found?
                → steal token → DONE
        NO:
            enumerate AD

2. do i have credentials?
    YES:
        re-enumerate as that user
    NO:
        Try:
            Kerberoast (Rubeus)
            AS-REP roast
        Look for creds

3. any ACL paths?
    YES:
        GenericWrite → take over
        WriteDACL → give yourself control → take over
        GenericAll → immediate control
    NO:
        keep enumerating / pivot

4. got a service account?
    YES:
        find where it runs
        go to that server
    NO:
        keep hunting creds / ACLs

5. on a server?
    YES:
        dump creds (Mimikatz)
        check sessions
        👉 DA present?
            → steal token → DONE
    NO:
        move to a better host

6. have delegation?
    YES:
        reset passwords of:
            service accounts
            IT users
            → pivot again

7. ADCS?
    YES:
        find vulnerable template
        → request cert as DA
        → DONE