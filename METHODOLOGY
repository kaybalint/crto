Always run:

whoami /all
ps
getuid
If LOCAL ADMIN:
Dump creds (Mimikatz / logonpasswords)
Check tokens (steal_token)
Check sessions (who’s logged in)

👉 Rule:

Local admin = credential access opportunity

🔍 Enumeration Priorities (in order)
Credentials (fastest win)
Sessions (DA logged in?)
ACL paths (BloodHound mindset)
Kerberos attack surface
🔐 Kerberos Attacks
Kerberoasting

Using Rubeus:

Target: users with SPNs
Weakness: TGS encrypted with service account hash
AS-REP Roasting
Condition: pre-auth disabled
Result: crackable AS-REP
Pass-the-Ticket (PtT)
Inject ticket → act as user
No password needed
🔐 ACL Abuse (CORE SKILL)
GenericWrite
Reset password
Add SPN → Kerberoast
Modify attributes
WriteDACL
Grant yourself GenericAll
Then take over object
GenericAll
Full control → instant takeover
👥 Group Abuse
Control group → add yourself
Have WriteDACL → modify ACL → then add yourself
🧠 Service Accounts (svc_*)

Target when:

SPNs exist
They run services
They have privileges

👉 Pattern:

Service account → server → creds → DA

🖥️ On a Server
Dump creds (Mimikatz)
Check sessions
Look for DA
Steal token

🎯 Token Theft
ps
steal_token <pid>
getuid

👉 Fastest win if DA session exists

🧪 ADCS (If present)

If template has:

Client Auth
Low-priv enrollment
“Supply in request”

👉 You can:

Request cert as DA → authenticate → full compromise

🧠 gMSA
Extract password from host
Use for lateral movement
Often high privilege
⚠️ Golden Rules
Don’t attack blindly
Follow attack paths, not tools
Credentials > enumeration
Deterministic > probabilistic

-------DECISION TREE--------

🟢 1. Foothold

👉 Local admin?

YES:
Dump creds (Mimikatz)
Check sessions
Check tokens

👉 DA session found?
→ steal token → DONE

NO:
Enumerate AD
🟡 2. Do I have credentials?
YES:
Re-enumerate as that user
Check:
Groups
ACLs
Access
NO:
Try:
Kerberoast (Rubeus)
AS-REP roast
Look for creds
🔵 3. Any ACL paths?
YES:
Abuse them:
GenericWrite → take over
WriteDACL → give yourself control → take over
GenericAll → immediate control
NO:
Keep enumerating / pivot
🟣 4. Got a service account?
YES:
Find where it runs
Go to that server
NO:
Keep hunting creds / ACLs
🔴 5. On a server?
YES:
Dump creds (Mimikatz)
Check sessions

👉 DA present?
→ steal token → DONE

NO:
Move to a better host
🟠 6. Have delegation (Helpdesk)?
YES:
Reset passwords of:
service accounts
IT users

→ pivot again

⚫ 7. ADCS?
YES:
Find vulnerable template
→ request cert as DA
→ DONE
🧠 Always Ask Yourself
Do I have creds?
Do I control something?
Can I reach a better machine?
Is a DA session available?

🔥 One-Line Mental Model
Creds → ACL → Service Account → Server → DA Session → Token → DA